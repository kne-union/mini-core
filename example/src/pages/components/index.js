import {Layout} from '@kne/mini-core';
import toolbar from '../../common/toolbar';
const Components = () => {
    return <Layout header={{title: '组件'}} toolBarList={toolbar}>组件</Layout>;
};

export default Components;
