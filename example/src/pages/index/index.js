import {Layout} from '@kne/mini-core';
import toolbar from '../../common/toolbar';

const Index = () => {
    return <Layout header={{title: '首页'}} toolBarList={toolbar}>首页</Layout>;
};

export default Index;
