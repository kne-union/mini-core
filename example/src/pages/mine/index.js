import {Layout} from '@kne/mini-core';
import toolbar from '../../common/toolbar';
const Mine = () => {
    return <Layout header={{title: '我的'}} toolBarList={toolbar}>我的</Layout>;
};

export default Mine;
